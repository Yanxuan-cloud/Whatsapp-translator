# 如何申请 Google Translate API Key

这份指南给选择了"Google Translate"作为翻译引擎的用户看，跟着做就行，全程大概 10 分钟。

> 如果觉得步骤太多，可以改用 DeepL 引擎——只要注册一个邮箱就能拿到免费 Key，不需要绑卡，更省事。

## 第一步：登录 Google Cloud

打开 [console.cloud.google.com](https://console.cloud.google.com)，用你的 Google（Gmail）账号登录。如果没有 Google 账号，先注册一个。

## 第二步：开通结算（绑定银行卡）

第一次使用 Google Cloud 会要求你绑定一张信用卡或借记卡。

- 新账号通常会自动获得 **$300 免费试用额度**，90 天内有效，用不完不退不补
- 绑卡时 Google 可能会扣 1 美元做验证，随后会自动退回，这是正常流程，不是被骗
- 试用额度用完或过期之前，Google 都会弹窗提醒确认才会开始正式扣费，不会啥都不提示就划走大笔钱

## 第三步：创建一个项目

登录后，点击页面顶部的"选择项目"，然后点"新建项目"。项目名字随便起，比如 `wa-translator`，创建即可。

## 第四步：启用 Cloud Translation API

1. 左侧菜单打开 "API 和服务" → "库"
2. 搜索框输入 `Cloud Translation API`
3. 点进去，点击"启用"按钮，等几秒钟生效

## 第五步：创建 API 密钥

1. 左侧菜单打开 "API 和服务" → "凭据"
2. 点击顶部"创建凭据" → 选择 "API 密钥"
3. 页面会生成一串字符（形如 `AIzaSy...`），这就是你要的 API Key，点"复制"

## 第六步（建议做，安全考虑）：给密钥加限制

1. 在刚才生成的密钥旁边点"编辑"（或点密钥名称进入详情页）
2. 找到"API 限制"，选择"限制密钥"
3. 在下拉列表里只勾选 **Cloud Translation API**
4. 保存

这样即使这个 Key 不小心泄露出去，别人也只能拿去调用翻译功能，不能用在你 Google Cloud 账号下的其他收费服务上，能少一层风险。

## 第七步：填进插件

复制这串 API Key，打开 WhatsApp 翻译插件的设置面板，翻译引擎选择 "Google Translate"，把 Key 粘贴进对应的输入框，保存即可。

## 常见问题

**绑卡会不会被乱扣费？**
不会自动扣。免费额度用完之前、以及后续真正产生费用前，Google 都会有明确提示，你可以随时在 Cloud Console 里设置"预算提醒"，超过某个用量百分比就发邮件通知你。

**没有信用卡怎么办？**
Google Cloud 的结算功能目前必须绑定信用卡或借记卡（Visa / MasterCard / American Express 均可），暂时没有免绑卡的申请通道。如果没有合适的卡，建议改用 DeepL 引擎。

**创建项目/开通结算时报错怎么办？**
部分地区或新注册的 Google 账号可能会遇到风控拦截，多为账号新、地区异常等原因触发。可以尝试换一张卡、隔一段时间再试，或者直接使用 DeepL 引擎作为替代方案。
