# 更新日志

格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)。

## [0.2.3] - 2026-09

### 修复（关键）
- 修复 DeepL 返回 403 "Missing Authorization header" 导致翻译全部失败的问题：API Key 改为通过 `Authorization: DeepL-Auth-Key` 请求头传递（同时保留表单参数兼容），适配 DeepL 当前认证要求和部分代理网络环境
- DeepL 429 限流错误增加中文说明

## [0.2.2] - 2026-09

### 新增
- 翻译失败不再"静默无反应"：失败原因直接显示在消息气泡内（悬停可看完整错误），并提供"重试"按钮
- 未配置 API Key 时，页面顶部显示配置引导横幅
- 控制台启动日志和诊断结果显示明确的版本号（如"已加载 v0.2.2"），用于确认 Chrome 实际运行的代码版本（排查"文件已覆盖但扩展未刷新/覆盖错文件夹"）
- 新增 `__waTranslate.testApi()`：在控制台直接发起一条测试翻译，验证"插件后台 → 翻译引擎 → 网络"整条链路
- `__waTranslate.debug()` 诊断表新增：引擎类型、DeepL/Google Key 是否已配置、成功/失败消息计数、Service Worker 连通性检查
- DeepL 403/401 错误明确提示"免费版 Key 必须用 api-free 主机"；DeepL 456 提示月度额度用尽
- 网络无法连接翻译 API 时明确提示检查 VPN/代理（Google API 国内通常无法直连）

### 修复
- 修复同一条消息在扫描中被重复发起翻译请求的问题（增加 pending 状态锁）
- 修复非文本消息（图片/语音）被永久标记为已处理、后续文本出现时不再翻译的问题
- 初始化流程增加 try/catch，任何一步失败都会显示可见提示而不是整个插件静默死亡
- 收窄 MutationObserver 全量扫描触发条件，降低对 WhatsApp 页面频繁 DOM 变动的性能消耗

## [0.2.1] - 2026-09

### 修复（关键）
- 修复 WhatsApp Web 改版后插件加载正常但翻译不出现的问题：消息/文本/输入框/标题选择器全部改为多策略匹配，优先使用抗变化的 `data-testid` / `role` 属性，再回退到传统 class
- 新增 `[role="row"]` 兜底识别：即使 WhatsApp 混淆掉 `message-in/out` class，仍能识别消息，并通过"已发送对勾图标"区分收发方向
- 修复 SPA 切换聊天后 MutationObserver 挂在已移除的 `#main` 节点上导致永久失效的问题，改为监听 `document.body`
- 修复打开聊天时已存在的历史消息不会被翻译的问题，现在启用时和切换聊天后会自动补扫
- 提取消息正文时排除引用回复内容，避免把被引用的消息一起送去翻译
- 深色模式适配补充 `data-theme="dark"` 选择器

### 新增
- 诊断工具：在 WhatsApp Web 页面控制台执行 `__waTranslate.debug()` 可查看消息节点、输入框、标题等识别情况，方便自助排查和反馈问题

## [0.2.0] - 2026-09

### 新增
- 首次使用风险告知弹窗，用户需明确同意后才会启动功能
- 每月字符额度保护，防止无意中超额消耗 API 额度
- DeepL / Google Translate 双引擎切换
- 引擎兼容性提示（选了 DeepL 不支持的语言时一键切换 Google）
- 按聊天对象记录语言，发消息时自动确定翻译方向
- 深色模式样式适配

### 变更
- 去掉了自动改写输入框 + 自动点击发送的高风险行为（v0.1.x 有）
- 发消息改为只提供译文建议 + 复制按钮，由用户手动粘贴发送
- API Key 拆分成每个引擎独立存储
- MutationObserver 监听范围从 `document.body` 缩小到 `#main` 主面板

### 修复
- `innerHTML` 全部替换为安全 DOM API（防 XSS）
- 内存缓存加上 LRU 上限，防止无限增长
- Google API Key 从 URL 查询参数改为请求头传递

## [0.1.0] - 早期版本

- 基础翻译功能：收消息显示翻译、发消息自动附带译文
- 仅支持 DeepL 引擎
- 自动发送行为（后因风险过高移除）
